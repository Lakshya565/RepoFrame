annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.5.20
charset-normalizer==3.4.7
click==8.4.1
colorama==0.4.6
fastapi==0.136.3
h11==0.16.0
idna==3.18
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
requests==2.34.2
starlette==1.2.1
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
uvicorn==0.49.0
openai==2.41.1
supabase>=2.0.0,<3.0.0
# PyJWT + its crypto extra are imported DIRECTLY by services/auth.py (ES256/RS256
# token verification) and services/github_app.py (RS256 app-JWT signing). They
# currently also arrive transitively via supabase-auth's `pyjwt[crypto]`
# dependency, but a direct import must be a direct requirement -- otherwise a
# future supabase change could silently break Phase 15 auth. The [crypto] extra
# pulls in `cryptography`, which the asymmetric algorithms require.
PyJWT[crypto]==2.13.0
# Imported directly for Fernet encryption of persisted GitHub user tokens.
cryptography==49.0.0
